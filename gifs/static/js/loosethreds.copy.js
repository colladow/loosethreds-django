// Generated by CoffeeScript 1.3.3
(function() {

  (function(loosethreds) {
    return loosethreds.copy = (function() {
      var inputBase, self;
      self = {};
      inputBase = $('<input type="text" class="input-xlarge image-url">');
      self.init = function() {
        var base;
        base = $('#user_images');
        base.delegate('button.copy', 'click', function(event) {
          var button, input;
          button = $(this);
          input = inputBase.clone();
          input.val(button.data('url'));
          input.attr('data-url', button.data('url'));
          button.replaceWith(input);
          return input.focus().select();
        });
        return base.delegate('input.image-url', 'blur', function(event) {
          var button, jnput;
          jnput = $(this);
          button = $('<button class="btn copy">Copy</button>');
          button.attr('data-url', input.data('url'));
          return input.replaceWith(button.clone());
        });
      };
      return self;
    })();
  })(loosethreds);

  $(document).ready(function() {
    return loosethreds.copy.init();
  });

}).call(this);
